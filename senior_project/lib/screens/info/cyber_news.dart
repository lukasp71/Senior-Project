import 'package:flutter/material.dart';

class CyberNewsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cyber News'),
      ),
      body: Center(
        child: Text('This is the Cyber News screen content.'),
      ),
    );
  }
}
