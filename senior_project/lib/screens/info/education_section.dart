import 'package:flutter/material.dart';

class EducationSectionsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Education Sections'),
      ),
      body: Center(
        child: Text('This is the Education Sections screen content.'),
      ),
    );
  }
}
