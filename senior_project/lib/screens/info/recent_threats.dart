import 'package:flutter/material.dart';

class RecentThreatsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Recent Threats'),
      ),
      body: Center(
        child: Text('This is the Recent Threats screen content.'),
      ),
    );
  }
}
